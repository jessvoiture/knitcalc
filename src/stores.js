import { writable } from 'svelte/store';

export const gaugeSts = writable(undefined);
export const gaugeRows = writable(undefined);

export const gaugeWidth = writable(undefined);
export const gaugeHeight = writable(undefined);

export const neckToArmpit = writable(undefined);
export const garmentWidth = writable(undefined);

export const neckCircumference = writable(undefined);

export const underarmCO = writable(undefined);
export const raglanCount = writable(undefined);
